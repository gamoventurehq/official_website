NEXT_PUBLIC_SITE_URL=https://www.gamoventure.in

# Server-only: configure privately in Vercel, never use NEXT_PUBLIC_ for credentials.
CONTACT_FORM_ENABLED=false
SMTP_USER=your-mailbox@example.com
SMTP_PASSWORD=replace-with-mailbox-password
